# FireWatch

### 소방 작전 의사결정 지원을 위한 셀룰러 오토마타 기반 건물 화재 확산 시뮬레이션 시스템

**Design Document**

* **학번:** 22012106
* **이름:** 이승민
* **E-mail:** dinola@naver.com
* **깃허브 주소** : https://github.com/dinnow123/FireWatch

## Revision History

| Revision date | Version # | Description | Author |
| --- | --- | --- | --- |
| 2026.05.28 | 0.1 | First Design Document | 이승민 |


## Contents

1. Introduction
2. Class Diagram
3. Sequence Diagram
4. State Machine Diagram
5. Implementation Requirements
6. Glossary
7. References

---

## 1. Introduction

본 문서는 Conceptualization, Analysis 단계에 이은 FireWatch 시스템의 세 번째 산출물이다. Analysis 단계에서 식별한 11개의 Use Case와 14개의 도메인 클래스를 바탕으로, 실제 구현에 직접 관여하는 모든 요소의 윤곽을 구체적으로 확정한다.

### 1.1. 본 문서의 범위

본 문서는 다음 4종의 정적·동적 모델을 포함한다.

* **Class Diagram** — 시스템을 구성하는 클래스들의 속성, 메서드, 관계를 도식화하여 정의한다. Analysis 단계의 Domain analysis가 책임 단위로 클래스를 식별한 데 비해, 본 문서는 각 클래스의 속성 타입과 메서드 시그니처를 명시하여 구현에 직접 사용 가능한 수준으로 구체화한다.
* **Sequence Diagram** — 각 Use Case의 실행 흐름을 객체 간 메시지 교환의 시간 순서로 표현한다. Analysis 단계의 Main Success Scenario를 객체 단위로 분해하여 구현 시 호출 순서의 기준이 된다.
* **State Machine Diagram** — 시스템 전체의 상태 전이를 단일 다이어그램으로 표현한다.
* **Implementation Requirements** — 하드웨어, 소프트웨어, 비기능적 요구사항을 명세한다.

### 1.2. 설계 원칙

본 시스템의 설계는 다음 원칙을 따른다.

* **MVC 분리** — Model(Building, Cell, EnsembleResult 등 도메인 데이터), View(PyQt6 화면 클래스), Controller(CAEngine, EnsembleRunner 등 비즈니스 로직)를 명확히 분리하여 화면 변경이 시뮬레이션 로직에 영향을 주지 않도록 한다.
* **결정성과 확률성의 분리** — CA Engine은 알고리즘적으로 결정론적(같은 seed → 같은 결과)이며, 확률성은 seed의 다양화로만 달성된다. 이는 디버깅 재현성과 앙상블 시뮬레이션의 통계적 유의성을 동시에 확보하기 위함이다.
* **numpy 벡터화** — 셀별 Python 루프 대신 boolean mask와 슬라이싱을 활용한다. 100×100 격자에서도 단일 회차가 수 ms 내에 완료되어야 20-50회 앙상블이 수 초 안에 끝난다.
* **데이터와 동작의 분리** — Grid는 상태 배열을 보관할 뿐 자기 자신을 업데이트하지 않는다. Simulator가 Grid를 입력으로 받아 다음 상태를 계산한다. 이는 동일한 건물 상태에서 다양한 시나리오를 비교할 수 있게 해준다.

---

## 2. Class Diagram

본 절은 FireWatch 시스템을 구성하는 클래스들을 정의한다. Analysis의 14개 도메인 클래스를 기반으로 하되, 구현 편의를 위해 일부 클래스를 분할하거나 통합하였다 (예: EnsembleResult는 numpy 배열로 표현되어 별도 클래스로 두지 않고 EnsembleRunner의 멤버 변수로 흡수, Grid는 Floor의 구현체로 도입).

### 2.1. 전체 Class Diagram

전체 클래스 다이어그램은 모델링 도구 StarUML로 작성되었다. 그림은 5개의 패키지(actors, model, engine, view, controller)로 구분되며, 각 패키지 내 클래스들의 속성, 메서드, 그리고 패키지 간 의존 관계를 표시한다.

![FireWatch Class Diagram](images/class_diagram.png)

### 2.2. 클래스 상세 설명

#### 2.2.1. Actors 패키지

##### (1) User (Abstract)

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-id: str` | String | 사용자 식별자 |
| `-name: str` | String | 사용자 이름 |
| `+canControl(): bool` | abstract → bool | 제어 권한 보유 여부 (하위 클래스에서 구현) |
| `+canView(): bool` | → bool | 결과 조회 권한 (모두 True) |

User는 Commander와 FieldOperator의 상위 추상 클래스이다. 결과 조회 기능은 모든 사용자가 가지며, 제어 권한 여부는 하위 클래스에서 결정한다.

##### (2) Commander (User)

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `+canControl(): bool` | override → bool | 항상 True 반환 |

Commander는 신고 입력, 시뮬레이션 실행, 시나리오 비교, 초기화 등 모든 제어 권한을 가진다. View 측에서는 canControl()의 반환값에 따라 메뉴 활성화/비활성화를 결정한다.

##### (3) FieldOperator (User)

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `+canControl(): bool` | override → bool | 항상 False 반환 |

FieldOperator는 결과 조회와 자기 디바이스의 층 전환·시간 조작만 수행 가능하다.

---

#### 2.2.2. Model 패키지

##### (1) Building

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-id: str` | String | 건물 식별자 |
| `-name: str` | String | 건물명 |
| `-address: str` | String | 주소 |
| `-floors: list[Grid]` | list of Grid | 층별 격자 객체 리스트 |
| `-equipment: list[FireSafetyEquipment]` | list | 소방 설비 목록 |
| `-connections: list[ConnectionCell]` | list | 층간 연결 셀 목록 |
| `+addFloor(grid: Grid): None` | | 층 추가 |
| `+addConnection(conn: ConnectionCell): None` | | 층간 연결 추가 |
| `+ignite(floor: int, x: int, y: int): None` | | 지정 층 셀에 발화점 설정 |
| `+copy(): Building` | | 깊은 복사 (앙상블 회차 독립성 보장) |

Building은 시뮬레이션의 정적 입력이다. 모든 회차에서 동일하게 사용되며, 각 회차가 자신만의 상태를 갖도록 copy()로 깊은 복사를 제공한다.

##### (2) Grid (Floor의 구현)

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-height: int` | int | 격자 행 수 |
| `-width: int` | int | 격자 열 수 |
| `-state: ndarray[int8]` | (H, W) | 셀별 현재 상태 (EMPTY/BURNING/BURNED/BLOCKED) |
| `-material: ndarray[int8]` | (H, W) | 셀별 재질 ID |
| `-heat: ndarray[float32]` | (H, W) | 셀별 누적 열량 |
| `-burn_timer: ndarray[int16]` | (H, W) | 셀별 연소 경과 tick |
| `-sprinkler: ndarray[bool]` | (H, W) | 셀별 스프링클러 유무 |
| `-shutter: ndarray[bool]` | (H, W) | 셀별 방화셔터 유무 |
| `+setWall(x: int, y: int): None` | | 벽 설정 (BLOCKED 고정) |
| `+setMaterial(x: int, y: int, mat_id: int): None` | | 재질 설정 |
| `+setSprinkler(x: int, y: int): None` | | 스프링클러 위치 등록 |
| `+setShutter(x: int, y: int): None` | | 방화셔터 위치 등록 |
| `+ignite(x: int, y: int): None` | | 셀을 BURNING으로 전환 |
| `+copy(): Grid` | | 모든 numpy 배열 깊은 복사 |

Grid는 한 층의 모든 셀 데이터를 numpy 배열로 보관하는 컨테이너이다. Analysis 단계의 Floor 클래스와 Cell 클래스가 합쳐진 구현 형태로, 셀별 객체 생성 비용을 피하고 벡터화 연산을 활용하기 위함이다.

##### (3) Cell (상수 모듈)

| **상수** | **값** | **의미** |
|---|---|---|
| `EMPTY` | 0 | 발화 가능, 아직 점화되지 않음 |
| `BURNING` | 1 | 현재 발화 중 (이웃에 heat 전달) |
| `BURNED` | 2 | 연소 완료 (더 이상 heat 전달 X) |
| `BLOCKED` | 3 | 발화 불가 (벽, 외부, 닫힌 셔터) |

Cell은 클래스가 아닌 상태 값 상수의 모음이다. Grid의 state 배열에 저장되는 정수 값으로, 각 셀이 시뮬레이션 중 가질 수 있는 4가지 동작 상태를 나타낸다.

##### (4) Material (Enum-like 모듈)

| **상수** | **속성** | **설명** |
|---|---|---|
| `MAT_CONCRETE` | (mult=0.05, thr=50, dur=100) | 콘크리트 (불연재) |
| `MAT_GYPSUM` | (mult=0.40, thr=20, dur=30) | 석고보드 |
| `MAT_WOOD` | (mult=1.20, thr=8, dur=15) | 목재 (기본값) |
| `MAT_TEXTILE` | (mult=1.80, thr=4, dur=8) | 직물·카펫 |
| `MAT_PLASTIC` | (mult=1.50, thr=6, dur=10) | 플라스틱 |
| `MAT_PAPER` | (mult=1.60, thr=3, dur=5) | 종이 |

| **메서드** | **시그니처** | **설명** |
|---|---|---|
| `+getProperty(mat_id: int): MaterialProperty` | | ID로 (배수, 임계값, 연소 지속) 조회 |
| `+numMaterials(): int` | | 등록된 재질 수 |

MaterialProperty 는 frozen dataclass로 `ignition_multiplier`, `ignition_threshold`, `burn_duration`을 보유한다. 셀별 재질은 정수 ID로 Grid.material에 저장되고, 시뮬레이션 시작 시 캐싱된 numpy 배열로 빠르게 조회된다.

##### (5) FireSafetyEquipment

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-type: str` | "sprinkler" / "shutter" | 설비 종류 |
| `-floor: str` | String | 설치 층 식별자 |
| `-x: int` | int | x 좌표 |
| `-y: int` | int | y 좌표 |

FireSafetyEquipment는 건물의 정적 정보로, Database에 저장되며 시뮬레이션 시작 시 Grid의 sprinkler / shutter 배열로 변환된다. 작동 여부는 본 클래스가 아닌 ScenarioConfig에서 결정한다.

##### (6) ConnectionCell

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-floor_a: int` | int | 연결의 한쪽 층 인덱스 |
| `-x_a: int, -y_a: int` | int, int | 한쪽 좌표 |
| `-floor_b: int` | int | 반대쪽 층 인덱스 |
| `-x_b: int, -y_b: int` | int, int | 반대쪽 좌표 |
| `-up_weight: float = 0.8` | float | 굴뚝 효과 (아래→위) 가중치 |
| `-down_weight: float = 0.2` | float | 하향 열 전달 가중치 |

ConnectionCell은 계단실·엘리베이터 샤프트 등 층간 화재 전파 경로를 표현한다. 상승 열 전달이 하강보다 강하다는 굴뚝 효과를 가중치로 표현한다.

##### (7) Report

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-building_id: str` | String | 신고된 건물 식별자 |
| `-ignition_floor: str` | String | 발화 층 (예: "1F", "B1") |
| `-ignition_x: int` | int | 발화점 x 좌표 |
| `-ignition_y: int` | int | 발화점 y 좌표 |
| `-ignition_time: datetime` | datetime | 발화 시각 |

Report는 UC1 Input Report에서 생성되는 신고 정보 객체이다. 시뮬레이션의 입력으로 사용된다.

##### (8) ScenarioConfig (SimParameters)

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-sprinkler_active: bool = True` | bool | 스프링클러 작동 여부 |
| `-shutter_active: bool = True` | bool | 방화셔터 작동 여부 |
| `-exit_closed: bool = False` | bool | 비상문 닫힘 여부 |
| `-vent_active: bool = False` | bool | 환기 시스템 작동 여부 |
| `-sprinkler_cooling: float = 8.0` | float | 스프링클러 한 tick당 heat 감소량 |
| `-shutter_trigger_heat: float = 5.0` | float | 셔터 자동 작동 임계 heat |
| `-exit_closed_multiplier: float = 1.03` | float | 비상문 닫힘 시 확산 가중 |
| `-vent_multiplier: float = 1.10` | float | 환기 작동 시 확산 가중 |

ScenarioConfig는 Analysis의 SimParameters에 대응하는 클래스로, dataclass로 구현된다. 4개 boolean과 효과 강도 파라미터를 가지며, 시나리오 간 비교의 단위가 된다.

##### (9) Scenario

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-config: ScenarioConfig` | ScenarioConfig | 시나리오 입력 파라미터 |
| `-result: EnsembleResult` | EnsembleResult | 앙상블 실행 결과 |
| `-label: str` | String | 시나리오 라벨 (예: "기준", "비교") |

Scenario는 하나의 ScenarioConfig와 그 결과인 EnsembleResult를 하나의 단위로 묶는다. UC10 Compare Scenarios에서 두 Scenario 간 셀별 확률 차이(Δ)를 산출하는 비교 단위가 된다.

---

#### 2.2.3. Engine 패키지

##### (1) Simulator (CAEngine)

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-building: Building` | Building | 시뮬레이션 대상 건물 (깊은 복사본) |
| `-scenario: ScenarioConfig` | ScenarioConfig | 시나리오 설정 |
| `-rng: Generator` | numpy.random.Generator | seed 기반 난수 생성기 |
| `-tick_count: int` | int | 누적 tick 수 |
| `+step(): None` | | 1 tick 진행 (모든 층 동기 업데이트) |
| `+run(max_ticks: int): None` | | max_ticks 또는 BURNING 소진까지 실행 |
| `+getFireMap(): list[ndarray]` | → list of bool array | 각 층의 (BURNING|BURNED) 마스크 |
| `+hasBurningCells(): bool` | → bool | 조기 종료 판단 |
| `-stepFloor(grid, threshold, mult, dur): None` | | 단일 층 1 tick 업데이트 |
| `-stepInterFloor(): None` | | 층간 ConnectionCell 통한 heat 전달 |

Simulator는 단일 회차의 시뮬레이션을 담당한다. 매 tick마다 (1) 8방향 heat 전달 → (2) 층간 heat 전달 → (3) 스프링클러 작동 → (4) 방화셔터 작동 → (5) 확률적 발화 → (6) 연소 타이머 증가 순으로 동기 업데이트한다. 동일 seed 입력 시 결정론적으로 동일한 결과를 산출한다.

##### (2) EnsembleRunner

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-building: Building` | Building | 공통 건물 |
| `-scenario: ScenarioConfig` | ScenarioConfig | 공통 시나리오 |
| `-n_runs: int = 20` | int | 앙상블 실행 횟수 |
| `-fire_counts: list[ndarray]` | list of int array | 각 층의 셀별 점화 누적 |
| `-tick_counts: list[int]` | list of int | 각 회차의 종료 tick |
| `+run(max_ticks: int, seed_base: int): None` | | n_runs 회 독립 실행 |
| `+probabilityMap(floor_idx: int): ndarray` | → float array | 해당 층의 셀별 확률 (0.0~1.0) |
| `+allProbabilityMaps(): ndarray` | → (F, H, W) | 전체 층 확률 |

EnsembleRunner는 Simulator를 동일 조건으로 n_runs회 반복 호출하고, 각 회차에서 셀별 점화 여부를 누적한다. 누적값을 n_runs로 나누어 셀별 화재 도달 확률 분포를 산출한다. seed_base + i를 회차별 seed로 사용하여 회차 간 독립성과 전체 재현성을 동시에 확보한다.

##### (3) EnsembleResult

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-probability_maps: ndarray` | (F, T, H, W) float32 | 층×시점×셀 도달 확률 |
| `-n_runs: int` | int | 산출에 사용된 회차 수 |
| `-tick_counts: list[int]` | list of int | 회차별 종료 tick |
| `+getProbabilityAt(floor: int, t: int): ndarray` | → (H, W) | 특정 층·시점의 확률 맵 |
| `+computeSummary(): dict` | → dict | 면적, 평균 확률, 위험 셀 수 등 요약 |
| `+diff(other: EnsembleResult): ndarray` | → (F, H, W) | 다른 결과와의 셀별 차이 |

EnsembleResult는 EnsembleRunner의 출력을 보관하는 데이터 객체이다. UC5(히트맵), UC8(단면도), UC9(결과 요약), UC10(시나리오 비교)에서 모두 본 객체를 읽어 시각화에 활용한다.

---

#### 2.2.4. Controller 패키지

##### (1) ReportController

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-current_report: Report` | Report | 현재 입력 중인 신고 |
| `+createReport(address: str, floor: str, x: int, y: int, time: datetime): Report` | | 신고 객체 생성·검증 |
| `+validate(report: Report): bool` | | 발화 좌표가 ROOM 셀인지 확인 |

##### (2) BuildingLoader

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-db: DatabaseManager` | DatabaseManager | DB 접근 객체 |
| `+loadByAddress(address: str): Building` | | 주소로 건물 조회 |
| `+listEquipment(building: Building): list` | | 등록된 소방 설비 목록 |

##### (3) ScenarioController

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-current_config: ScenarioConfig` | | 현재 설정 중인 시나리오 파라미터 |
| `+setSprinkler(active: bool): None` | | 스프링클러 작동 여부 설정 |
| `+setShutter(active: bool): None` | | 방화셔터 작동 여부 설정 |
| `+setExitClosed(closed: bool): None` | | 비상문 상태 설정 |
| `+setVent(active: bool): None` | | 환기 시스템 상태 설정 |
| `+reset(): None` | | 모든 파라미터를 기본값(True)으로 초기화 |
| `+getConfig(): ScenarioConfig` | | 현재 설정 반환 |

##### (4) SimulationController

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-runner: EnsembleRunner` | | 앙상블 실행기 |
| `-current_result: EnsembleResult` | | 마지막 시뮬레이션 결과 |
| `+runEnsemble(building: Building, config: ScenarioConfig, n_runs: int): EnsembleResult` | | UC4 본체 — 앙상블 실행 |
| `+cancel(): None` | | 진행 중 시뮬레이션 중단 |
| `+compareScenarios(scen_a: Scenario, scen_b: Scenario): ndarray` | | 두 시나리오 결과의 셀별 차이 |
| `+reset(): None` | | UC11 — 메모리상 결과 폐기 |

SimulationController는 UC4 Run Ensemble과 UC10 Compare Scenarios의 본체 로직을 담당한다. View와 Model·Engine을 잇는 중간 계층이다.

##### (5) DatabaseManager

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-conn: Connection` | | DB 연결 객체 |
| `+findBuilding(address: str): Building` | | 주소로 건물 조회 |
| `+loadFloorplan(building_id: str): ndarray` | | 평면도 셀맵 로드 |
| `+loadEquipment(building_id: str): list[FireSafetyEquipment]` | | 소방 설비 위치 로드 |
| `+saveResult(scenario: Scenario): bool` | | 시뮬레이션 결과 저장 |
| `+loadResult(scenario_id: str): EnsembleResult` | | 저장된 결과 로드 |

---

#### 2.2.5. View 패키지

##### (1) MainWindow

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-sidebar: QListWidget` | | 메뉴 사이드바 |
| `-stacked_view: QStackedWidget` | | 화면 전환용 스택 |
| `-mode_toggle: QToolButton` | | Commander / FieldOperator 모드 토글 |
| `+switchView(name: str): None` | | 화면 전환 |
| `+setMode(user: User): None` | | 사용자 권한에 따라 메뉴 활성화 |

##### (2) ReportInputView

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-address_edit: QLineEdit` | | 주소 입력 필드 |
| `-floorplan_widget: FloorplanWidget` | | 평면도 표시 위젯 |
| `-floor_combo: QComboBox` | | 발화 층 선택 드롭다운 |
| `-time_edit: QDateTimeEdit` | | 발화 시각 |
| `+onSearch(): None` | | 주소 검색 핸들러 |
| `+onCellClick(x: int, y: int): None` | | 평면도 셀 클릭 핸들러 |
| `+onSubmit(): Report` | | 신고 등록 |

##### (3) ParametersView

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-sprinkler_check: QCheckBox` | | 스프링클러 체크박스 |
| `-shutter_check: QCheckBox` | | 방화셔터 체크박스 |
| `-exit_check: QCheckBox` | | 비상문 체크박스 |
| `-vent_check: QCheckBox` | | 환기 시스템 체크박스 |
| `+onSave(): ScenarioConfig` | | 파라미터 저장 |
| `+onDefault(): None` | | 기본값(모두 작동)으로 초기화 |

##### (4) SimulationView

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-progress_bar: QProgressBar` | | 진행 표시 |
| `-log_text: QTextEdit` | | 로그 출력 영역 |
| `-cancel_btn: QPushButton` | | 취소 버튼 |
| `+onStart(): None` | | 시뮬레이션 시작 |
| `+onProgress(i: int, total: int): None` | | 진행 콜백 |

##### (5) HeatmapView

| **속성/메서드** | **타입/시그니처** | **설명** |
|---|---|---|
| `-floor_tabs: QTabBar` | | 층 선택 탭 |
| `-time_slider: QSlider` | | 시간 슬라이더 |
| `-grid_widget: HeatmapGridWidget` | | 히트맵 그리드 |
| `-colorbar: ColorbarWidget` | | 색상 범례 |
| `+onFloorChange(idx: int): None` | | UC6 — 층 전환 |
| `+onTimeChange(t: int): None` | | UC7 — 시점 전환 |

##### (6) SectionView, SummaryView, ComparisonView

각 화면은 UC8(단면도), UC9(결과 요약), UC10(시나리오 비교)에 1:1 대응되는 View 클래스로, EnsembleResult를 입력으로 받아 시각화한다.

---

## 3. Sequence Diagram

본 절은 Analysis 단계의 11개 Use Case 중 핵심 6개에 대한 Sequence Diagram을 제시한다. 모델링 도구는 StarUML을 사용하였다. 각 다이어그램은 객체 간 메시지 교환의 시간 순서를 표현하며, alt 프래그먼트는 분기, loop 프래그먼트는 반복을 나타낸다.

### 3.1. UC1 + UC2 — Input Report & Load Building

![Sequence Diagram - Input Report](images/seq_input_report.png)

관제관이 ReportInputView에서 주소를 입력하면, ReportController가 BuildingLoader.loadByAddress()를 호출한다. BuildingLoader는 DatabaseManager를 통해 건물을 조회하고, 결과가 있으면 Building 객체를 반환한다. 미등록 건물인 경우 ErrorMessage를 띄우고 재입력을 요청한다. 건물이 로드되면 평면도가 ReportInputView에 표시되며, 관제관이 평면도에서 발화 셀을 클릭하면 ReportController가 Report 객체를 생성한다. 발화 좌표가 ROOM이 아닌 경우(벽 등) 가장 가까운 ROOM 셀로 자동 보정된다.

### 3.2. UC3 — Set Parameters

![Sequence Diagram - Set Parameters](images/seq_set_parameters.png)

관제관이 ParametersView에서 4개 체크박스(스프링클러, 방화셔터, 비상문, 환기)를 조작하면, 각 체크박스의 toggle 시그널이 ScenarioController의 setSprinkler() 등을 호출한다. "저장" 클릭 시 ScenarioController.getConfig()가 ScenarioConfig 객체를 반환하며, 이는 SimulationView로 전달된다. 별도 설정 없이 패널을 닫은 경우(3a) 기본값(모두 True)으로 진행한다.

### 3.3. UC4 — Run Ensemble

![Sequence Diagram - Run Ensemble](images/seq_run_ensemble.png)

관제관이 SimulationView의 시작 버튼을 누르면 SimulationController.runEnsemble()이 호출된다. SimulationController는 EnsembleRunner를 생성하고 run() 메서드를 호출한다. EnsembleRunner는 내부에서 N회(20-50) 루프를 돌며 매 회차마다 새로운 Simulator를 seed=seed_base+i로 생성하고, Simulator.run()을 호출한다. 각 회차의 Simulator는 매 tick마다 step()을 호출하여 8방향 heat 전달 → 층간 heat 전달 → 스프링클러 → 방화셔터 → 확률적 발화 → 연소 타이머 순으로 업데이트한다. 모든 회차 완료 후 EnsembleResult를 산출하여 SimulationController에 반환하며, SimulationController는 결과를 DatabaseManager.saveResult()로 저장하고 HeatmapView로 자동 전환한다. 사용자가 도중 취소 버튼을 누르면(2a) cancel() 플래그가 설정되고 다음 tick 시작 시 루프가 break된다.

### 3.4. UC5 + UC6 + UC7 — Display Heatmap, Switch Floor, Control Time

![Sequence Diagram - Heatmap](images/seq_heatmap.png)

UC4 완료 직후 자동으로 HeatmapView가 활성화된다. HeatmapView는 EnsembleResult.getProbabilityAt(floor=발화층, t=0)을 호출하여 초기 확률 맵을 가져오고, HeatmapGridWidget에 셀별 색상을 그라데이션으로 렌더링한다. 사용자가 층 탭을 클릭하면(UC6) onFloorChange()가 호출되어 새 층의 확률 맵으로 다시 렌더링한다. 시간 슬라이더 조작 시(UC7) onTimeChange()가 호출되어 해당 시점의 확률 맵으로 갱신된다. 두 경우 모두 EnsembleResult는 재계산되지 않고 이미 산출된 4D 배열에서 슬라이싱만 수행하므로 1초 이내에 응답한다.

### 3.5. UC9 — View Result Summary

![Sequence Diagram - Summary](images/seq_summary.png)

사용자가 SummaryView를 열면 EnsembleResult.computeSummary()가 호출된다. 본 메서드는 (1) 화재 확산 면적: 도달 확률 > 0인 셀 수, (2) 영향 받은 층 수: 도달 확률 > 0인 셀이 하나라도 있는 층의 수, (3) 평균 도달 확률, (4) 위험 셀 수: 도달 확률 ≥ 70%인 셀 수를 계산하여 dict로 반환한다. SummaryView는 이를 좌측 통계 패널과 우측 차트(line chart, bar chart)로 표시한다.

### 3.6. UC10 — Compare Scenarios

![Sequence Diagram - Compare Scenarios](images/seq_compare.png)

관제관이 ComparisonView의 4개 체크박스를 조정한 후 "비교 실행"을 누르면 SimulationController.compareScenarios()가 호출된다. 본 메서드는 새 ScenarioConfig로 EnsembleRunner를 다시 실행하여 새 EnsembleResult를 산출하고, 기존 결과(scen_a.result)와 새 결과(scen_b.result)의 셀별 차이(Δ = result_b - result_a)를 numpy 배열 뺄셈으로 계산한다. ComparisonView는 좌·우 히트맵을 동일한 층/시점으로 동기화 표시하고, 하단에 Δ 그리드를 발산형 색상(빨강: Δ>0 비교가 더 위험, 파랑: Δ<0 비교가 더 안전)으로 시각화한다.

---

## 4. State Machine Diagram

본 절은 FireWatch 시스템 전체의 상태 전이를 단일 다이어그램으로 표현한다.

![FireWatch State Machine Diagram](images/state_machine.png)

### 4.1. 상태 설명

| **State** | **설명** |
|---|---|
| **Launch System** | 앱이 실행되어 메인 화면을 표시하는 상태. 좌측 사이드바와 모드 토글이 활성화된다. |
| **Waiting for Report Input** | UC1 화면이 활성화되어 관제관의 신고 정보 입력을 기다리는 상태. |
| **Loading Building** | UC2 — 입력된 주소로 DatabaseManager를 통해 건물 데이터를 조회하는 상태. |
| **Building Load Error** | 미등록 건물 또는 DB 연결 실패. ErrorMessage를 띄우고 Waiting for Report Input으로 복귀한다. |
| **Report Registered** | UC1 완료 — Report 객체가 생성되고 ParametersView로 자동 전환되는 상태. |
| **Waiting for Parameters** | UC3 화면에서 4개 boolean의 설정을 기다리는 상태. |
| **Running Ensemble** | UC4 — EnsembleRunner가 N회의 시뮬레이션을 순차 실행 중인 상태. ProgressBar가 진행을 표시한다. |
| **Simulation Canceled** | 관제관이 도중 취소 — 진행 결과를 폐기하고 Waiting for Parameters로 복귀한다. |
| **Ensemble Completed** | 모든 회차 종료 — EnsembleResult가 DB에 저장되고 HeatmapView로 자동 전환된다. |
| **Displaying Heatmap** | UC5 — 히트맵이 표시된 상태. UC6(층 전환), UC7(시간 조작)으로 분기 가능. |
| **Displaying Section** | UC8 — 건물 단면도가 표시된 상태. |
| **Displaying Summary** | UC9 — 정량적 요약이 표시된 상태. |
| **Comparing Scenarios** | UC10 — 두 시나리오 결과가 좌우로 나란히 비교되는 상태. 내부에서 Running Ensemble을 1회 더 실행한다. |
| **Field Operator Mode** | Commander 전용 메뉴가 비활성화된 읽기 전용 모드. Displaying Heatmap/Section/Summary로만 진입 가능. |
| **Reset** | UC11 — 메모리상 결과와 입력값을 폐기하고 Launch System으로 복귀한다. |

### 4.2. 주요 전이

* `Launch System → Waiting for Report Input` : 사이드바에서 "신고 입력" 클릭
* `Waiting for Report Input → Loading Building` : 주소 검색 버튼 클릭
* `Loading Building → Building Load Error` : DB에 일치 건물 없음 / 연결 실패
* `Loading Building → Report Registered` : 건물 로드 성공 후 발화점 지정·신고 등록 완료
* `Report Registered → Waiting for Parameters` : 자동 전환
* `Waiting for Parameters → Running Ensemble` : "저장" 또는 "기본값으로 진행" 클릭
* `Running Ensemble → Simulation Canceled` : 취소 버튼 클릭
* `Running Ensemble → Ensemble Completed` : N회 완료
* `Ensemble Completed → Displaying Heatmap` : 자동 전환
* `Displaying Heatmap ↔ Displaying Section ↔ Displaying Summary` : 사이드바 메뉴 선택 (양방향)
* `Displaying Heatmap → Comparing Scenarios` : "시나리오 비교" 메뉴 → 내부 Running Ensemble → 좌우 결과 표시
* 어느 상태에서든 `→ Reset` : 사이드바 "초기화" 클릭 → 확인 다이얼로그 → 확인 시 Launch System으로 복귀
* 어느 상태에서든 `→ Field Operator Mode` : 모드 토글 전환. Field Operator 모드에서는 Commander 전용 메뉴로의 전이가 비활성화된다.

---

## 5. Implementation Requirements

### 5.1. Hardware Requirements

| **항목** | **요구사항** |
|---|---|
| **CPU** | Intel Core i5 (2.4GHz, 4코어) 이상 — 100×100 격자 × 20회 앙상블 기준 20초 이내 완료 |
| **RAM** | 4GB 이상 — 4D 확률 배열 (3층 × 60시점 × 100×100 × 4byte ≈ 7MB) + PyQt6 런타임 |
| **HDD or SSD** | 500MB 이상 여유공간 — 앱 본체 + 건물 데이터베이스 + 시뮬레이션 결과 캐시 |
| **Display** | 1280×800 이상 권장 — 사이드바·히트맵·시간 슬라이더가 한 화면에 들어가야 함 |
| **Network** | 불필요 (모든 데이터는 로컬 DB) |

### 5.2. Software Requirements

| **항목** | **요구사항** |
|---|---|
| **Operating System** | macOS 12 이상 / Windows 10 이상 / Ubuntu 20.04 이상 |
| **Implementation Language** | Python 3.10 이상 |
| **GUI Framework** | PyQt6 6.5 이상 |
| **연산 라이브러리** | numpy 1.24 이상 |
| **차트 라이브러리** | matplotlib 3.7 이상 (line chart, bar chart 용) |
| **Font** | IBM Plex Sans KR / IBM Plex Mono (시스템 fallback 시 기본 sans-serif / monospace) |
| **모델링 도구** | StarUML 6.x (Class / Sequence / State Machine Diagram 작성) |

### 5.3. Nonfunctional Requirements

* **성능** — 100×100 격자, 20회 앙상블 기준 시뮬레이션이 20초 이내 완료되어야 한다. 히트맵 / 단면도 / 요약 화면의 렌더링은 1초 이내에 완료되어야 한다.
* **재현성** — 동일한 (Building, ScenarioConfig, seed_base, n_runs) 입력에 대해 항상 동일한 EnsembleResult를 산출해야 한다(알고리즘적 결정성).
* **사용성** — 긴박한 현장 상황에서도 직관적으로 사용 가능하도록, 신고 입력 → 시뮬레이션 실행까지 최대 3회 클릭으로 완료될 수 있어야 한다.
* **데이터 무결성** — DatabaseManager.saveResult() 실패 시 결과는 메모리상에 유지되며, 경고 메시지로 사용자에게 알린다.
* **확장성** — 격자 해상도 증가, 회차 수 확대, 병렬 실행 도입 시 EnsembleRunner.run()의 내부 루프만 수정하면 되도록 인터페이스를 보존한다.

---

## 6. Glossary

| 용어 | 설명 |
| --- | --- |
| **Class Diagram** | 객체지향 시스템 설계에서 클래스의 속성, 메서드, 클래스 간 관계를 도식으로 표현한 UML 다이어그램. |
| **Sequence Diagram** | 시스템 내 객체들 사이의 메시지 교환을 시간 순서대로 표현하는 UML 다이어그램. |
| **State Machine Diagram** | 시스템이 가질 수 있는 상태와 상태 간 전이를 표현하는 UML 다이어그램. |
| **MVC (Model-View-Controller)** | 데이터(Model), 화면(View), 제어 로직(Controller)을 분리하는 소프트웨어 아키텍처 패턴. |
| **dataclass** | Python의 데이터 컨테이너 클래스를 간결하게 정의할 수 있게 해주는 표준 라이브러리 기능. `@dataclass` 데코레이터로 선언한다. |
| **numpy** | Python의 수치 연산 라이브러리. 다차원 배열과 벡터화 연산을 지원하여 셀별 Python 루프 없이 격자 전체를 한 번에 처리할 수 있다. |
| **PyQt6** | Python에서 Qt6 GUI 프레임워크를 사용할 수 있게 해주는 바인딩 라이브러리. |
| **boolean mask** | numpy 배열에서 조건을 만족하는 셀만 선택하는 기법. `arr[condition_array]` 형태로 사용. |
| **seed** | 의사난수 생성기의 초기 상태값. 동일한 seed는 동일한 난수 시퀀스를 생성하므로 재현성을 보장한다. |
| **동기 업데이트 (Synchronous Update)** | CA에서 모든 셀의 다음 상태를 직전 tick의 스냅샷을 기준으로 동시에 계산하는 방식. |
| **굴뚝 효과 (Stack Effect)** | 화재 시 가열된 공기가 부력에 의해 상승하는 현상. 본 시스템에서는 ConnectionCell의 up_weight > down_weight로 표현된다. |
| **인식론적 결정론 (Epistemic Determinism)** | 시스템 자체는 결정론적이지만 관측자의 지식이 불완전하므로 출력을 확률 분포로 표현하는 입장. 본 시뮬레이션의 확률값은 우주의 무작위성이 아니라 모델러의 무지를 정량화한 값이다. |
| **앙상블 시뮬레이션 (Ensemble Simulation)** | 확률적 모델을 복수 실행하고, 결과의 통계적 분포로부터 확률적 예측을 도출하는 방법론. |
| **StarUML** | UML 다이어그램 작성용 모델링 도구. |

---

## 7. References

1. Object Management Group, "Unified Modeling Language (UML) Specification, Version 2.5.1", 2017 — https://www.omg.org/spec/UML/
2. Riverbank Computing, "PyQt6 Reference Guide", 2024 — https://www.riverbankcomputing.com/static/Docs/PyQt6/
3. NumPy Developers, "NumPy User Guide v1.26", 2024 — https://numpy.org/doc/stable/
4. StarUML, "StarUML Documentation", 2024 — https://docs.staruml.io/
5. Karafyllidis, I., Thanailakis, A., "A model for predicting forest fire spreading using cellular automata", Ecological Modelling, 99(1), 1997 — 본 시스템의 heat 누적 + 확률적 발화 2단계 모델의 학술적 근거.
6. Trucchia, A. et al., "PROPAGATOR: An Operational Cellular-Automata Based Wildfire Simulator", Fire, 3(3), 2020 — 확률적 CA + 앙상블 방법론의 실증 사례.
7. Python Software Foundation, "dataclasses — Data Classes", Python 3.10 Standard Library — https://docs.python.org/3/library/dataclasses.html
